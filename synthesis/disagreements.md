# Disagreements — index

Derived, not stored. Each entry cites the insight IDs on each side so it can be
re-verified and can go stale visibly. Regenerated from insights, never patched.

**Status: empty.** Requires at least two creators processed. Currently one (pilot).
